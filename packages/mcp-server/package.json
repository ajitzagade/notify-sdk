{
  "name": "@orgname/notify-mcp-server",
  "version": "0.1.0",
  "description": "MCP server exposing a tenant's notify-sdk /v1 REST API to Claude, Cursor, and other MCP-compatible AI assistants — read-only by default, opt-in writes.",
  "main": "dist/index.js",
  "bin": {
    "notify-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  }
}
